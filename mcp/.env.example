# Copy to .env and fill in real values. NEVER commit .env.
MCP_PORT=4000
B1_API_URL=http://localhost:3001
LOG_LEVEL=info

# Solana
RPC_URL=https://api.devnet.solana.com

# Hivework Anchor program (Group A). Leave empty until A ships.
# When set, signing tools build unsigned txs. When empty, they return status="pending_program".
HIVEWORK_PROGRAM_ID=