// TODO: implement once Prisma client exists.
// Checks: ref_code exists in leaves_metadata, ≥1 prior click in clicks, value > 0.
export async function validate(_pendingId: string): Promise<boolean> {
  return false
}
